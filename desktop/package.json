{
  "name": "coachbuild-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "CoachBuild desktop shell — owns the window, speaks the companion wire contract",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && electron dist/main.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.3.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
