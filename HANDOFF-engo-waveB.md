<!-- merged into HANDOFF.md 2026-07-24 12:54:44Z; previous content preserved there. Append new rounds below. -->

## Addendum — post-audit FIX-THEN-SHIP pass (P3 cleanups, engo scope)

Audit confirmed two P3 dead-code items in my Wave B scope. Both fixed.

1. **`components/hextech/patchMoversFormat.ts` deleted** (`git rm`), along with its only remaining consumer, `components/__tests__/patchMoversFormat.test.ts` (`git rm`) — grep-confirmed its ONLY importer repo-wide was that test file; `MoverRow.tsx`/`app/movers/page.tsx` use their own local helpers (see the original entry above's "Conflict discovered" section). The follow-up decision flagged there (wire it back in vs. delete) is now resolved as "delete."

2. **`components/hextech/homeSearch.ts` dead player-mode branch removed**: `SearchMode`, `PlayerSubject` (+ `TrackedPlayerSubject`/`LinkPlayerSubject`), `deriveMainView`, `modeAfterPlayerSelect`, `isProsSearchEmpty`, `canFavoritePlayerSubject`, `defaultSourceForPlayer`, `trackedSubjectFromPlayerRef`, `subjectFromPendingPlayerSelect`, `wireViewForPlayer` — all deleted, plus their corresponding `describe` blocks in `components/__tests__/homeSearch.test.ts`. Grep-verified (imports, not string mentions) before touching anything: repo-wide, only `app/page.tsx` imports from this module, and it imports exactly `applyWireMainView`, `wireViewForChampion`, `defaultSourceForKind`, `type WireMainView` — confirmed via direct read that its only consumption of `applyWireMainView`'s return value is `.activeLane`/`.champ` (never `.searchMode`/`.selectedPlayer`/`.tab`/`.gamesSource`), which is what made the rest of this safe.
   - **Necessary knock-on edits, not separately called out by the auditor but required to keep `.tsx` files (out of scope, untouched) compiling**: `MainView` narrowed to its only-ever-live shape (`{kind:"champion", champ, lane}` — the `| {kind:"player", ...}` arm depended on the now-deleted `PlayerSubject`); `HomeRestoreState.selectedPlayer?: PlayerSubject` field dropped entirely (nothing produces it anymore); `applyWireMainView`'s body simplified to drop the now-unreachable player branch; `modeAfterLaneChange`/`modeAfterChampionSelect`'s return type changed from the deleted `SearchMode` union to the literal `"champions"` (both already only ever returned that value) — these two functions weren't on the audit's explicit removal list and aren't imported live either, so left in place rather than also deleting them (that's a separate, not-yet-audited call).
   - **Deliberately kept, not scope creep**: `HomeRestoreState.searchMode: "champions"` (a literal now, not the deleted `SearchMode` type) was kept rather than removed outright, purely so every surviving champion-branch test's `toEqual` assertion needed zero edits beyond deleting the dead-branch blocks — genuinely mechanical, not an opportunity taken to also prune it.
   - `components/__tests__/homeSearch.test.ts`: also trimmed the mixed `describe("lane changes keep the champion (issue 2)")` block down to only its surviving (`wireViewForChampion`/`applyWireMainView`-only) test, and stripped two now-invalid `.selectedPlayer` assertions from the one surviving champion-branch `applyWireMainView` test (the field no longer exists on the type).

**Verification**: `npx tsc --noEmit` → clean. `npx vitest run` → 110 files / 1515 tests, all green (down from 111/1555 — exactly the deleted test file + deleted describe blocks, no new failures). `npx eslint` on both touched files → 0 errors/warnings.
