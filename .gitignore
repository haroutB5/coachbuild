node_modules/
.next/
out/
/build/
.vercel/
.env*.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

.vercel

# --- managed by push-ai-to-github: secrets + build artifacts ---
.env
.env.*
!.env.example
!.env*.example
node_modules/
.next/
.turbo/
.cache/
dist/
/build/
out/
coverage/
.vercel/
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
[Bb]in/
[Oo]bj/
artifacts/
.claude/worktrees/
scripts/.backfill-team-comps-reorder-cursor.json

.vercel

# live-client capture evidence (scripts/capture-live-client.ps1)
_capture/

# scratch data pulled down by ad-hoc Riot/LCU probes at the repo root — real
# PUUIDs / Riot IDs, never meant to be committed (found tracked 2026-07-31)
/acct.json
/acct2.json
/chal.json
/cm.json
/gm.json
/ids.json
/ids2.json
/lv.json
/m.json
/ma.json
/t.json

# session handoffs / one-off audit + investigation notes — regenerated per
# session, never a durable artifact worth versioning
HANDOFF.md
HANDOFF-*.md
AUDIT-*.md
INVESTIGATION-*.md

# Throwaway diagnostic probes from the 2026-08-15 overlay investigation.
# _tmp-probe/ holds a self-signed cert + PRIVATE KEY for the fake Live Client
# Data server used to drive the overlay without League — never commit these.
# Kept on disk (the safety gate blocks recursive deletion) but out of git.
/_tmp-probe/
/overlay-host/_tmp-*.mjs
